# Google Gemini API key (free at https://aistudio.google.com) — powers the M4 Citizen Shield.
# Without it, the shield falls back to a transparent rule-based engine (marked "rules" in the UI).
GEMINI_API_KEY=
