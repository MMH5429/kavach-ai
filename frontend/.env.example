# Base URL of the Kavach backend API (no trailing slash).
# Local dev: leave unset (defaults to http://localhost:8000).
# Deploy: set to your deployed backend origin.
VITE_API_URL=http://localhost:8000
